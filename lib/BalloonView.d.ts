import CanvasRenderView from "./CanvasRenderView";
export default class BalloonView extends CanvasRenderView {
    private textfield;
    scroll(x: number): void;
    on(event: "select", callback: Function): any;
    on(event: string, callback: Function): any;
}
